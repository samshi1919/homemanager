<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zuoer.mapper.SysConfigMapper">

    <resultMap id="SysConfigMap" type="com.zuoer.domain.entity.SysConfig">
        <result column="config_id" property="configId"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--通过查询实体作为筛选条件查询-->
    <select id="listSysConfig" resultType="com.zuoer.domain.vo.SysConfigVO">
        select *
        from sys_config
        <where>
            1=1
            <if test="queryParams.configId != null">
                and config_id = #{queryParams.configId}
            </if>
            <if test="queryParams.configKey != null and queryParams.configKey != ''">
                and config_key = #{queryParams.configKey}
            </if>
            <if test="queryParams.configValue != null and queryParams.configValue != ''">
                and config_value = #{queryParams.configValue}
            </if>
            <if test="queryParams.createTime != null">
                and create_time = #{queryParams.createTime}
            </if>
            <if test="queryParams.updateTime != null">
                and update_time = #{queryParams.updateTime}
            </if>
        </where>
    </select>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="configId" useGeneratedKeys="true">
        insert into sys_config(config_key, config_value, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.configKey}, #{entity.configValue}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="configId" useGeneratedKeys="true">
        insert into sys_config(config_key, config_value, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.configKey}, #{entity.configValue}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
         config_key = values(config_key) , config_value = values(config_value) , create_time = values(create_time) , update_time = values(update_time)     </insert>

</mapper>

